# Prometheus 配置文件，定義了 Prometheus 的全局設置和抓取配置
global:
  scrape_interval: 5s
# 抓取配置，定義了 Prometheus 如何抓取數據
scrape_configs:
  - job_name: 'otel-collector'
    # 使用 OTLP 協議從 Grafana Alloy 收集數據
    static_configs:
      - targets: ['alloy:4318'] # 指向 Alloy 的 OTLP 轉接埠