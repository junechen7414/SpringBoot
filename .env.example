# 實際檔名.env，內容與 .env.example 相同，但變數值替換為實際的 Oracle 資料庫帳號和密碼。
# 開發環境 Oracle 資料庫
ORACLE_DEV_USERNAME=your_dev_username
ORACLE_DEV_PASSWORD=your_dev_password

# 資料庫主機名（dev profile 用）。本地用 IDE / bootRun 直連時「不需要設定」，
# application-dev.yml 會自動 fallback 成 localhost。
# 透過 podman/docker compose 在容器內跑時，compose 會自動帶入 oracle-db（服務名），不必在此設定。
# ORACLE_DB_HOST=localhost

# 測試環境 Oracle 資料庫
ORACLE_TEST_USERNAME=your_test_username
ORACLE_TEST_PASSWORD=your_test_password

# HTTP Basic 認證（Spring Security）。本地 bootRun 未設定時，application.yml 有 dev 友善預設值。
# 正式/共享環境務必覆蓋為強密碼。
# api-*：一般 API 呼叫端；internal-*：內部 *Client loopback 自呼叫用的服務帳號。
# 注意：這兩組是「機器帳號」，密碼在應用內以 {noop} 前綴逐字比對、刻意不做雜湊
#（機器帳號 + env 明文來源，雜湊對不上威脅模型卻要付每請求成本；見 SecurityConfig 註解
# 與 docs/security-external-idp-migration.md）。真正的使用者認證請走外部 IdP。
API_USERNAME=api
API_PASSWORD=change_me_api
INTERNAL_USERNAME=internal
INTERNAL_PASSWORD=change_me_internal