- 封裝 (Encapsulation) - 使用 `private` **欄位** 與 `public` **方法** 來保護數據。

- 繼承 (Inheritance) - Player 類別繼承 Character。

- 多型 (Polymorphism) - attack() 方法在 Player 和 Enemy 類別中有不同的實作。

- 抽象 (Abstraction) - Character 為抽象類別，定義 attack() 但由子類別實作。
